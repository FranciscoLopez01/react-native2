import React from "react";

const pgreet = ({ title }) => {
  return (
    <section>
      <div>
        <h1>{title}</h1>
        <p>Bienvenido a la clase de CoderHouse de ReactNative!</p>
        <button>Boton</button>
      </div>
    </section>
  );
};

export default pgreet;
